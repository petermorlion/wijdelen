namespace WijDelen.UserImport.ViewModels {
    /// <summary>
    /// Indicates how the imported users are linked to a group.
    /// </summary>
    public enum UserImportLinkMode {
        New,
        Existing
    }
}